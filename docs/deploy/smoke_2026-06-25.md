# dev 배포 스모크 테스트 — 2026-06-25

## 목적
- `dev` push → CI(ci.yml `push: [main, dev]`) 동작 검증
- `dev` 머지 시 Vercel **dev-writing.pullim.ai 재배포** 트리거 → 회전한 `ANTHROPIC_API_KEY`(Preview scope) 반영
- 재배포 후 `dev-writing.pullim.ai/api/score`의 503(E8) 해소 확인

## 전제
- Vercel Preview scope에 **회전한 새 키**가 `ANTHROPIC_API_KEY`로 설정돼 있어야 함
  (env만 바꾸면 기존 배포 미반영 → 본 머지가 재배포 트리거 역할)

## 검증 체크 (머지 후)
- [ ] dev 브랜치 CI green (typecheck·unit·components·e2e)
- [ ] dev-writing.pullim.ai 재배포 완료
- [ ] dev-writing.pullim.ai/api/score → 200 (유효 토큰)

## 운영값 근거 (2026-06-25 — docs/29 §27 갱신)
- 2026-06-25 도메인 alias 설정(P0-#2 closure): **운영 `writing.pullim.ai`(→production/main)** · **스테이징 `dev-writing.pullim.ai`(→dev 브랜치)**
- pullim.ai NS = `ns1/ns2.vercel-dns.com`(Vercel DNS 직접 호스팅) → 도메인 Add 시 레코드 자동 생성, TLS 발급 완료
- 실측: `writing.pullim.ai` GET / → 200 · `POST /api/score`(유효 토큰) → **200** / `dev-writing.pullim.ai` 도달(SSO 보호)
- 정본 갱신: [docs/29_decisions_intake_2026-06-08.md:27](../29_decisions_intake_2026-06-08.md)의 "기본 Vercel URL·alias 없음"은 06-08 시점 기준 → **본 도메인 alias 설정으로 superseded**(후속 docs/29 본문 정정 필요)

## 파이프라인
local → **dev**(staging, dev-writing.pullim.ai) → main(production, writing.pullim.ai)

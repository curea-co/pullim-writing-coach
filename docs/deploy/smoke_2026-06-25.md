# dev 배포 스모크 테스트 — 2026-06-25

## 목적
- `dev` 머지로 Vercel **dev-writing 재배포**를 트리거해, Preview scope에 설정한 회전 키(`ANTHROPIC_API_KEY`)를 반영
- `ci.yml` `push: [main, dev]` 동작 검증 (dev push에 CI가 도는지)

## 전제
- Vercel **Preview scope**에 새 `ANTHROPIC_API_KEY` 설정 완료 (env 변경은 재배포 시점에 반영됨 → 본 머지가 재배포 트리거)

## 검증 (머지 후)
- [ ] dev 브랜치 CI green
- [ ] dev-writing 재배포 완료
- [ ] dev-writing `/api/score` 정상 응답
  - dev-writing은 Vercel Deployment Protection(SSO) 뒤이므로, SSO 로그인 상태의 브라우저에서 확인

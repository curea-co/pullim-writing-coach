# 대표님 보고 — 2026-06-05 (목) 10:30

> **대상**: 대표님
> **보고자**: 최선혜
> **기준 작업일**: 2026-06-04 (목, 휴일 회복 후 복귀 day)

---

## 요약

> **트랙 A — `pullim-writing-coach` (v1, 출시 준비 prod)**: 학생/교사가 메타(학년·과목·장르·분량)를 직접 입력해 5영역 채점을 받는 본 trunk. 안정성과 출시 준비(P0 차단 해소·테스트 회귀·운영 가시성)가 우선입니다.
>
> **트랙 B — `pullim-writing-coach_v2` (v2, 새 패러다임)**: 안내서 한 장을 던지면 AI가 메타·조건·루브릭을 자동 추출해 준비/평가 모드로 분기하는 새 trunk. CEO 재설계 doc §3을 코드로 옮긴 라이브 AI 프로토타입입니다.

- **트랙 A**:
  1. ★ **사용자가 오류를 만나면 운영팀이 즉시 알 수 있게** 자동 알림 시스템을 붙였습니다. 출시 전 반드시 해야 하던 6건 중 1건을 자체적으로 끝냈습니다.
  2. 크롬·파이어폭스·사파리 **세 브라우저에서 자동으로 회귀 테스트**가 돌도록 확장했습니다. 출시 후 "내 브라우저에선 안 되는데요?" 사고를 사전에 막는 안전망입니다.
  3. 신규 개발자가 들어와도 30분 안에 환경을 맞출 수 있도록 **설정 템플릿 파일**을 정리했습니다.

- **트랙 B**:
  1. 휴대폰에서 잘 보이도록 **어두운 화면(다크) 모드**·**햄버거 메뉴**·**스마트폰 화면처럼 보이는 입력 영역** 3건을 추가했습니다.
  2. 데모 비밀번호 입력을 **화면 가운데 팝업**으로 바꿔 자연스럽게 안내했습니다.
  3. 채점 대기 중 문구를 **"AI가 5영역으로 채점 중"**으로 바꿔 사용자가 무엇이 진행 중인지 알 수 있게 했습니다.
  4. ★ 소개 페이지를 "시각 시뮬레이션(가짜)"에서 **"실제 AI가 동작 중"**으로 사실에 맞게 갱신했습니다. (안내서 자동 정리와 5영역 채점 모두 진짜로 동작합니다.)
  5. 앞으로 코드가 바뀔 때마다 **자동으로 점검**해 주는 시스템(Codex 리뷰)을 붙였습니다.

핵심은 **트랙 A의 출시 준비가 P0 자력 해소 가능 항목을 모두 닫은 상태**라는 점, 그리고 **트랙 B에서 "시각 프로토타입"으로 안내되던 화면이 실제로는 안내서 자동 추출(`/api/extract`)·5영역 채점(`/api/score`) 모두 라이브 LLM(Claude Haiku)으로 동작**하고 있음을 사실에 맞게 정정했다는 점입니다.

---

## 1. 두 화면 직접 비교 — v1 ↔ v2 (먼저 확인 부탁드립니다)

핵심 차이를 한 문장씩으로:
- **v1은 "메타 입력 → 채점"** 패러다임으로 출시 준비가 진행 중인 본 trunk
- **v2는 "안내서 던지기 → AI 자동 정리 → 모드 분기"** 패러다임의 새 trunk로, 핵심 AI는 이미 라이브, 입력 파서·STT는 단계적 구현 중

두 라인은 같은 채점 엔진(`/api/score` Claude Haiku, 루브릭 v0.5)을 공유합니다. v2는 v1의 lib 7파일을 100% 재사용한 위에 새 UI/UX 패러다임을 얹은 구조입니다.

- **v1 (트랙 A, 출시 준비 prod)**: <https://pullim-demo.vercel.app/>
- **v2 (트랙 B, 새 패러다임)**: <https://pullim-writing-coachv2.vercel.app/>

| 비교 항목 | v1 (`pullim-demo.vercel.app/try`) | v2 (`pullim-writing-coachv2.vercel.app/`) |
|---|---|---|
| **진입 화면** | 별도 페이지에서 데모 토큰 입력 게이트 통과 후 진입 (`/try`) | 메인 페이지에서 안내서 던지기 폼 즉시 노출, 데모 토큰은 모달로 |
| **입력 패러다임** | 메타(학년·과목·장르·분량) 직접 입력 → 글 입력 → 채점 (3-step wizard) | 안내서 한 장 던지면 AI가 메타·조건·루브릭 자동 추출 → 준비/평가 모드 분기 |
| **AI 안내서 추출** | 없음 (학생/교사가 메타 직접 입력) | ✅ 라이브 (`/api/extract` Claude Haiku) |
| **AI 5영역 채점** | ✅ 라이브 (`/api/score` Claude Haiku, 루브릭 v0.5) | ✅ 라이브 (동일 엔드포인트·루브릭 100% 재사용) |
| **입력 채널 수** | 4채널 (붙여넣기 · DnD · TXT/DOCX 업로드 · 클립보드 자동 감지) | 6채널 — Universal Capture (사진 · 파일 · 붙여넣기 · 링크 · 말(STT) · 타이핑) |
| **데모 비밀번호 UI** | 별도 페이지 토큰 입력 후 sessionStorage 보존 | 메인 화면 모달 — 토큰 필요한 액션에서 즉시 등장 |
| **다크 모드** | 없음 (라이트만) | ✅ 풀림 네이비 팔레트 + 사용자 토글 |
| **모바일 대응** | 햄버거 LNB + 1열 카드 (iPhone 14 Pro × 7 라우트 깨짐 0건 회귀 완료) | 햄버거 메뉴 + 전역 내비(GlobalNav) + 입력 영역 스마트폰 스타일 |
| **결과 화면** | 5영역 점수 + 자동저장 + 결과 LRU 최대 20건 + `/results/[id]` 영구 링크 + PDF 내보내기 | 5영역 점수 (라이브) / mock 폴백 가능 |
| **에러 모니터링** | ✅ Sentry 통합 (PR #60, 서버·edge·브라우저 4 layer) | 미통합 |
| **CI 자동화** | components + e2e (chromium·firefox·webkit 3브라우저) + Codex Review | Codex Review (PR #12) |
| **위치(repo)** | `curea-co/pullim-writing-coach` | `curea-co/pullim-writing-coach_v2` |
| **출시 마일스톤** | 6/15 D-11 (출시 준비 prod) | 새 패러다임 검증 단계 (CEO doc §3 재설계 기반) |



---

## 2. 트랙 A — `pullim-writing-coach` (출시 준비 prod) 3 PR

| PR | 핵심 | 영향 |
|---|---|---|
| **#60** | Sentry 에러 모니터링 통합 — Next.js 16 instrumentation 4 layer(server·error.tsx·global-error·API catch) + PII 차단 + 무료 tier 안전 샘플링 | ★ **P0-#5 출시 차단 1건 자력 해소**. 가입·DSN 등록은 EPO 자체 진행 (무료 tier, 30분 소요, 대표님 결정 불필요) |
| **#59** | Playwright 3브라우저(chromium·firefox·webkit) E2E 매트릭스 27건 통과 | P1-#9 CI gate 강화 + P2-firefox/webkit 닫힘 |
| **#61** | `.env.example` 6 placeholder + `.gitignore` `!.env.example` 예외 | infra/docs 정비 (Sentry env 동기화 입력) |

**부수 학습**: #60은 Codex 봇 3 라운드(정정 5건) 후 APPROVED — 인프라/관측성 PR이 UX PR보다 검수 깊이 평균 2~3배. #59는 1차 push 후 캐시 키 누락 사고(webkit 바이너리 부재) → `-cfw` suffix fix로 30분 안에 그린.

---

## 3. 트랙 B — `pullim-writing-coach_v2` (새 패러다임) 7 PR

| PR | 핵심 | 카테고리 |
|---|---|---|
| **#6** | 소개 페이지: "mock 시각 프로토타입" → **"라이브 추출·채점 동작 중"** 상태 사실 정정 | 상태 안내 정정 |
| **#7** | 채점 대기 문구: "AI가 5영역으로 채점 중" (무엇이 진행 중인지 가시화) | UI/UX |
| **#8** | 데모 비밀번호 입력: 상단 배너 → **모달 방식**으로 전환 | 데모 게이트 개편 |
| **#9** | **다크 모드** 추가 (풀림 네이비 팔레트) + 사용자 토글 | UI/UX |
| **#10** | 모바일 햄버거 내비게이션 + 테마 전환 깜빡임(하이드레이션) 수정 | 모바일·UX |
| **#11** | 글 입력 영역을 **스마트폰 액정 스타일**로 디자인 (모바일 사용 맥락 정합) | UI/UX |
| **#12** | **Codex Review CI 워크플로우** (GitHub Actions self-hosted) | 개발 인프라 |

**핵심 메시지**: PR #6에서 명시했듯 v2는 더 이상 "시각 프로토타입"이 아닙니다. **안내서 자동 추출(`/api/extract`)·5영역 채점(`/api/score`) 모두 Claude Haiku 라이브 동작** 중이며, 입력 파서(OCR·HWP·STT)만 단계적 구현 중입니다.

---

## 4. 출시 차단 P0/P1/P2 13건 진척 정리 (2026-06-02 기준 출시 계획 대비)

### 완료 (5건, 모두 어제~6/2 사이 자력 해소)
- **P1-#9** CI 워크플로 강화 (components + e2e gate) — PR #54·#59
- **P1-#10** 모바일 반응형 정밀 검증 (iPhone 14 Pro × 7 라우트 깨짐 0건) — 6/2 prod 회귀
- **P2** PDF 캔버스 슬라이싱 (파일 사이즈 N→1배) — PR #58
- **P2** firefox/webkit E2E 확장 — PR #59
- **P0-#5** 에러 모니터링 (Sentry 통합) — PR #60 ★ **출시 차단 1건 자력 해소**

### 부분/후퇴 (2건)
- **P1-#7** HWP 파싱 — 한컴 hwp.js 라이브러리가 5년 미유지 상태라 클라 파싱은 포기, 대신 사용자에게 "한컴오피스에서 DOCX로 저장 후 올려 주세요" 친절한 안내 가이드로 후퇴(PR #53·#55). 실제 HWP 파싱은 사진 OCR(Naver Clova) 도입 시점에 재검토 필요합니다.
- **P0-#6** 출시 전 dogfood — 6/2에 EPO 단독 모바일 회귀 1건만 진행. 학생 5명·교사 2명 실제 모집은 아직 시작 못 한 상태입니다.

### 미완 (8건, 출시 차단)
- **P0-#1** Vercel Hobby → 조직·Pro 이관 — 대표님 결제 결정 대기
- **P0-#2** 공식 도메인 alias(`writing.pullim.ai`) Vercel 연결 — 부모 사이트 DNS 관리 대기
- **P0-#3** 부모 인증 SDK 통합(auth.pullim.ai SSO 또는 실 토큰) — 부모 인증 시스템 준비 완료 대기
- **P0-#4** 부모 billing 통합(크레딧/구독 또는 Free-only rate-limit) — 대표님 결정 대기
- **P1-#8** 사진 OCR 파일럿(Naver Clova) — Clova API key 발급 대기
- **P1-#11** /about 부모 도메인 링크 — 부모 도메인 URL 확정 대기
- **P2** 링크 본문 추출(D 채널) — 자력 진행 가능, M3 W2 plan 확정 후 즉시 진입 예정

---

## 5. 회고 — 2026-06-02 머지된 작업 (v1 9 PR + v2 5 PR = 합계 14 PR)

6/4 작업의 기준선이 되는 6/2 sprint day. 같은 day에 두 트랙이 동시에 PR을 머지한 첫 패턴입니다. 출처: GitHub `curea-co/pullim-writing-coach/pulls` · `curea-co/pullim-writing-coach_v2/pulls` (merged: 2026-06-02 기준).

### 5-1. 트랙 A — `pullim-writing-coach` (v1) 9 PR

**테스트·CI 기반 구축 (4건)**
- **PR #50** — Vitest + RTL 셋업 + 컴포넌트 단위 테스트 4건 (트랙 A 회귀/PR A)
- **PR #51** — Playwright + /try E2E 회귀 4건 (트랙 A — PR B)
- **PR #52** — /try E2E +5 시나리오 (트랙 A 회귀 확장, 총 9건)
- **PR #54** — components + e2e 잡 CI 워크플로 추가 (Vitest + Playwright)

6/2 이전엔 회귀 가시성이 단위 테스트만 있었고, 이 4 PR로 **컴포넌트 단위 테스트·Playwright E2E·CI 자동 실행** 3축이 모두 박혔습니다. 이후 6/4의 #59(firefox/webkit 매트릭스)·#60(Sentry) 머지도 이 기반 위에 가능했습니다.

**사용자 입력 UX (1건)**
- **PR #53** — HWP/HWPX 친절한 변환 가이드 (클라 파싱 ecosystem 대응)

한컴 hwp.js 라이브러리가 5년 미유지 상태라 직접 파싱은 포기, "한컴오피스에서 DOCX로 저장 후 올려 주세요" 안내로 후퇴.

**Codex 누적 정정 (1건)**
- **PR #55** — Codex PR #51/#52/#53 미해결 thread 4건 정정 + HWP UX 제거

테스트·HWP UX 도입 직후 Codex 봇이 잡은 thread를 한 PR에 일괄 정정.

**신규 UX 기능 (3건)**
- **PR #56** — `/me` LRU 시각화 카드 (#9 UX polish) — 사용자가 자주 쓰는 메타 학습 패턴을 `/me` 페이지에서 가시화
- **PR #57** — `ScoreForm` 통합 테스트 9건 — full wizard (#11 UX polish) — 전체 동선(Step 1·2·3 + handleResubmit + draft 복원) 회귀
- **PR #58** — PDF 멀티페이지 캔버스 슬라이싱 (#10 UX polish) — 파일 사이즈 N→1배 절감

(※ PR #56·#57은 6/2 머지 후 Codex가 추가 thread를 남겨, 6/4 별 PR(정정 commit) 머지가 필요했던 이력 있습니다.)

### 5-2. 트랙 B — `pullim-writing-coach_v2` (v2) 5 PR

v2 repo가 6/2 신축되면서 같은 day에 첫 5 PR이 머지됐습니다. 핵심은 **3·4번 PR로 안내서 자동 추출·5영역 채점이 실제 LLM(Claude Haiku)로 라이브 동작**하게 된 점입니다.

- **PR #1** — `fix(qa)`: 링크 채널·인라인 수정·모바일 여백 3건 수정
  - v2 신축 직후 첫 QA 회귀에서 잡힌 시각·동작 3건
- **PR #2** — `chore`: 화면 안내 텍스트 2건 삭제
  - 사용자 혼동 유발 안내 카피 정리
- **PR #3** — `feat`: 평가 UI ↔ 실제 `/api/score` 연결 (라이브 채점, 토글 게이트)
  - v2의 평가 모드가 mock에서 **실제 v1 채점 엔진과 같은 `/api/score`(Claude Haiku) 라이브 호출**로 전환. 라이브 토글로 mock/라이브 분기 가능
- **PR #4** — `feat`: 안내서 자동 추출 LLM화 (`/api/extract`)
  - "안내서 한 장 던지면 AI가 메타·조건·루브릭 자동 추출" 패러다임의 핵심 엔드포인트. Claude Haiku 활용
- **PR #5** — `docs`: README를 실제 프로젝트 문서로 교체
  - 신축 직후 README placeholder를 실제 v2 패러다임 안내로 갱신

### 5-3. 6/2 패턴 — "같은 day, 두 트랙, 14 PR"

- 같은 하루 안에 **v1 9 PR(테스트·CI·UX) + v2 5 PR(신축·라이브 AI)**이 동시에 머지된 첫 day입니다.
- 두 트랙이 같은 채점 엔진(`/api/score` Claude Haiku, 루브릭 v0.5)을 공유하기 때문에 가능했던 패턴이며, 6/4에도 동일 패턴(v1 3 PR + v2 7 PR = 10 PR)으로 반복됐습니다.
- 회의록 *6/1 evaluation* §능동성 피드백(주도적·차별화·선제적 구현)에 대한 직접 응답 형태로 본 sprint day가 잡혔습니다.

### 5-4. 6/2 산출 합계 (이번 보고서 시점까지)

| 트랙 | 6/2 PR | 6/4 PR | 합계 |
|---|---|---|---|
| v1 `pullim-writing-coach` | 9 | 3 | **12** |
| v2 `pullim-writing-coach_v2` | 5 | 7 | **12** |
| **양 트랙 합** | **14** | **10** | **24** |


